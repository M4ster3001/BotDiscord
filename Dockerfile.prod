FROM node:alpine

WORKDIR /usr/app

COPY package.json ./

RUN npm install

# RUN npm run build

RUN npm install -g pm2 \
  && pm2 install typescript \
  && pm2 start ecosystem.config.ts

COPY . .

EXPOSE 3333

CMD ["npm", "run", "production"]